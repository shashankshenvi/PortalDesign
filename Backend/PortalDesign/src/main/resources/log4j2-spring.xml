<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="DEBUG" monitorInterval="30">

    <Properties>
        <Property name="LOG_DIR">/App/log</Property>
        <!--        <Property name="LOG_DIR">logs</Property>-->
    </Properties>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{dd/MM/yyyy hh:mm a} %-5level Line Number : %line - %msg%n"/>
        </Console>

        <RollingFile name="AppLog" fileName="${LOG_DIR}/login_application.log"
                     filePattern="${LOG_DIR}/login_application-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="%d{dd/MM/yyyy hh:mm a} %-5level Line Number : %line - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>

        <RollingFile name="HibernateLog" fileName="${LOG_DIR}/form_hibernate.log"
                     filePattern="${LOG_DIR}/login_hibernate-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="%d{dd/MM/yyyy hh:mm a} %-5level Line Number : %line - %msg%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Hibernate SQL Logs -->
        <Logger name="org.hibernate.SQL" level="info" additivity="false">
            <AppenderRef ref="HibernateLog"/>
        </Logger>
        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="info" additivity="false">
            <AppenderRef ref="HibernateLog"/>
        </Logger>

        <!-- Project Logs -->
        <Logger name="org.Project" level="info"/>

        <!-- Root Logger -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppLog"/>
        </Root>
    </Loggers>
</Configuration>